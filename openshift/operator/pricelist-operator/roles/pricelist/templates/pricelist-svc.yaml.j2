apiVersion: v1
kind: Service
metadata:
  labels:
    app: "{{ meta.name }}-pricelist"
    tier: frontend
  name: "{{ meta.name }}-pricelist-frontend-svc"
  namespace: "{{ meta.namespace }}"
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: "{{ meta.name }}-pricelist"
    tier: frontend
status:
  loadBalancer: {}
