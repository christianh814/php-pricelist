apiVersion: v1
kind: Secret
metadata:
  labels:
    app: "{{ ansible_operator_meta.name }}-pricelist"
  name: "{{ ansible_operator_meta.name }}-pricelist-creds"
  namespace: "{{ ansible_operator_meta.namespace }}"
type: Opaque
data:
  database: "{{ database | b64encode }}"
  dbuser: "{{ dbuser | b64encode }}"
  dbpassword: "{{ dbpassword | b64encode }}"
