apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: "{{ ansible_operator_meta.name }}-pricelist"
    tier: database
  name: "{{ ansible_operator_meta.name }}-pricelist-db-svc"
  namespace: "{{ ansible_operator_meta.namespace }}"
spec:
  ports:
  - port: 3306
    protocol: TCP
    targetPort: 3306
  selector:
    app: "{{ ansible_operator_meta.name }}-pricelist"
    tier: database
status:
  loadBalancer: {}
