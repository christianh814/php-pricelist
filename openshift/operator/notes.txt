#### Create operator
operator-sdk new pricelist-operator --type=ansible --api-version=pricelist.chernand.io/v1alpha1 --kind=Pricelist --skip-git-init {--cluster-scoped}

#### Build in /home/chernand/Kubernetes/operators/pricelist-operator
sudo operator-sdk build quay.io/redhatworkshops/pricelist-operator:latest
docker push quay.io/redhatworkshops/pricelist-operator:latest

### Setup

Modify deploy/operator.yaml to replace the image with yours and set pull policy

# See https://github.com/operator-framework/operator-sdk/blob/master/doc/user-guide.md#create-a-new-project for additional modifications

#### AFTER BUILDING IT....
# Setup Service Account
oc create -f deploy/service_account.yaml

# Setup RBAC
oc create -f deploy/role.yaml
oc create -f deploy/role_binding.yaml

# Setup the CRD
oc create -f deploy/crds/<YOUR_APP>_crd.yaml

# Deploy the app-operator
oc create -f deploy/operator.yaml

# Define the CR
oc create -f deploy/crds/<YOUR_APP>_cr.yaml

